# Sanitized environment template for demo
# Fill with your own test values locally. DO NOT commit secrets.

# AI provider (placeholder)
AI_PROVIDER_API_KEY=
AI_PROVIDER_MODEL=gpt-4o-mini

# TON MCP / Wallet placeholders
TON_MCP_NETWORK=mainnet
TON_MCP_ENDPOINT=https://example-ton-mcp.local
TON_MCP_PRIVATE_KEY=    # set only in local env, never commit

# App settings
ENVIRONMENT=demo
DEBUG=true
